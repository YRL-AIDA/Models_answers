headers: (0,0);(0,1);(0,2);(0,3)

import pandas as pd

df = pd.DataFrame({
    '': ['Number of QRS detected out of possible 3000'],
    'Non-Syntactic': ['2850'],
    'CC': ['2799'],
    'Multi-component based CC': ['2931'],
}, index=[0])
